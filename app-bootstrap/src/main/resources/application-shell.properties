# Spring Shell Profile Configuration

# Enable Spring Shell for terminal mode
spring.shell.interactive.enabled=true
spring.shell.command.version.enabled=false

# Shell-specific logging
logging.level.org.springframework.shell=INFO
