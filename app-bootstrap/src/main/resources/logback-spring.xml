<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Import Spring Boot configuration -->
    <springProperty scope="context" name="APP_DATA_DIR" source="app.data.directory" defaultValue=".pomodoro-timer"/>
    <springProperty scope="context" name="APP_LOG_FILE" source="app.data.log-file" defaultValue="timer-app.log"/>

    <!-- Define the log file location -->
    <property name="LOG_DIR" value="${user.home}/${APP_DATA_DIR}"/>
    <property name="LOG_FILE" value="${LOG_DIR}/${APP_LOG_FILE}"/>

    <!-- Console appender for development -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Rolling file appender -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>

        <!-- Rolling policy -->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- Daily rollover with .gz compression -->
            <fileNamePattern>${LOG_DIR}/timer-app.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <!-- Each file should be at most 10MB -->
            <maxFileSize>10MB</maxFileSize>
            <!-- Keep 30 days worth of history -->
            <maxHistory>30</maxHistory>
            <!-- Total size of all archive files, if exceeded, oldest files are deleted -->
            <totalSizeCap>300MB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- Root logger configuration -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

    <!-- Application-specific logger -->
    <logger name="com.jabaddon.timer" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </logger>

    <!-- Reduce noise from Spring Boot -->
    <logger name="org.springframework" level="WARN"/>
    <logger name="org.springframework.boot" level="INFO"/>
</configuration>
